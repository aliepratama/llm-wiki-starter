# LLM Wiki Configuration

## Active Modules
Check the boxes below `[x]` to persistently enable modules across all AI chat sessions. 
The AI Agent will automatically read this list whenever you start a new chat, ensuring your preferences are remembered without having to re-prompt.

- [ ] `modules/knowledge-graph.md` - Enables the text-based Knowledge Graph (`wiki/graph/network.md`).
